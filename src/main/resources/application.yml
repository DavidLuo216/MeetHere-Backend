spring:
  datasource:
    url: jdbc:mysql://ecnuer996.cn:3306/venue_reservation_db?useUnicode=true&characterEncoding=UTF-8&serverTimezone=UTC&allowMultiQueries=true
    username: root
    password: MeetHere!password0
    driver-class-name: com.mysql.cj.jdbc.Driver

mybatis:
  mapper-locations: classpath:mapper/*.xml
  type-aliases-package: cn.ecnuer996.meetHereBackend.model

server:
  port: 8756


